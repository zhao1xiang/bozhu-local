# 清除浏览器缓存说明

## 🔄 为什么需要清除缓存

更新到 v2.2.3 版本后，前端代码已经修复了视力值类型转换问题，但浏览器可能还在使用旧版本的缓存文件，导致：
- 视力值仍然以数字类型发送
- 批量预约仍然出现422错误
- 新功能无法生效

## 🛠️ 解决方法

### 方法一：强制刷新（推荐）
**适用于**：Chrome、Edge、Firefox

1. 打开系统页面
2. 按下快捷键：
   - **Windows**: `Ctrl + Shift + R` 或 `Ctrl + F5`
   - **Mac**: `Cmd + Shift + R`
3. 等待页面完全重新加载
4. 重新登录并测试

### 方法二：清除浏览器缓存
**适用于**：所有浏览器

#### Chrome / Edge
1. 按 `Ctrl + Shift + Delete` 打开清除浏览数据窗口
2. 选择时间范围：**全部时间**
3. 勾选：
   - ✅ 缓存的图片和文件
   - ✅ Cookie 和其他网站数据（可选，会退出登录）
4. 点击"清除数据"
5. 关闭浏览器，重新打开
6. 访问系统并重新登录

#### Firefox
1. 按 `Ctrl + Shift + Delete` 打开清除历史记录窗口
2. 选择时间范围：**全部**
3. 勾选：
   - ✅ 缓存
   - ✅ Cookie（可选，会退出登录）
4. 点击"立即清除"
5. 关闭浏览器，重新打开
6. 访问系统并重新登录

### 方法三：无痕/隐私模式测试
**适用于**：快速验证是否是缓存问题

1. 打开浏览器的无痕/隐私模式：
   - **Chrome/Edge**: `Ctrl + Shift + N`
   - **Firefox**: `Ctrl + Shift + P`
2. 在无痕窗口中访问系统
3. 登录并测试功能
4. 如果正常，说明确实是缓存问题

### 方法四：禁用缓存（开发者选项）
**适用于**：开发测试

1. 按 `F12` 打开开发者工具
2. 切换到 **Network（网络）** 标签
3. 勾选 **Disable cache（禁用缓存）**
4. 保持开发者工具打开
5. 刷新页面（`F5` 或 `Ctrl + R`）

## ✅ 验证缓存已清除

### 检查前端版本
1. 打开系统页面
2. 按 `F12` 打开开发者工具
3. 切换到 **Console（控制台）** 标签
4. 查看是否有加载新文件的日志
5. 检查 **Network（网络）** 标签中的 JS 文件名是否更新

### 检查功能是否正常
1. 新增患者，输入视力值：1.2, 1.3
2. 保存患者
3. 创建预约
4. 如果不再出现422错误，说明缓存已清除成功

## 🎯 针对不同部署方式

### 本地版（backend_server.exe）
1. 关闭浏览器
2. 停止 backend_server.exe
3. 确认使用的是最新版本的 exe 文件
4. 重新启动 backend_server.exe
5. 清除浏览器缓存（使用上述方法）
6. 重新打开浏览器访问

### 服务器版
1. 确认服务器上的 frontend/ 目录已更新
2. 重启服务器（如果需要）
3. 客户端清除浏览器缓存
4. 重新访问系统

## 🔍 常见问题

### Q: 清除缓存后还是有问题？
A: 
1. 确认使用的是最新版本的程序
2.